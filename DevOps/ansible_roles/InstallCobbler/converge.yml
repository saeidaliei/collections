---
- name: Converge
  hosts: localhost
  connection: local
  gather_facts: true
  become: true
  become_user: root
  tasks:
    - name: Include orfeo.cobbler role
      ansible.builtin.include_role:
        name: orfeo.cobbler
